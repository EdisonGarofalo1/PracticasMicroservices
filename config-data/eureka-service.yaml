server:
  port: 8761
eureka:
  client:
    fetch-registry: false   # false porque no es cliente
    register-with-eureka: false    # false porque no es cliente
    #service-url:
     # default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/