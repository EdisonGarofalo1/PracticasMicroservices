spring:
  application:
    name: venta-service


server:
#  port: 8182
  #para que me de un uerto cualquiera que este disponible
  port: ${PORT:${SERVER_PORT:0}}

eureka:
  client:
    fetch-registry: true   # true porque es cliente
    register-with-eureka: true    # true porque  es cliente
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
#      hostname: localhost
      instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}